# Weather Application

This is an end-to-end weather application built with Node.js, Express.js, and vanilla JavaScript for the frontend. It will fetch weather data from a third-party API.

## Project Setup

1. Clone the repository.
2. Install dependencies: `npm install`
3. Run the application: `npm start` (or similar)
